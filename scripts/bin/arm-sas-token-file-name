#!/bin/bash
storage_account_name="${ARM_STORAGE_ACCOUNT_NAME:-undefined-storage-account-name}"
base_directory="${CREDENTIALS_DIRECTORY_AZURE:-${HOME}./credentials/azure}"
sas_token_file_name="${base_directory}/${storage_account_name}.sas-token"

echo "${sas_token_file_name}"
