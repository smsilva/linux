#!/bin/bash
cat <<EOF
AWS_DEFAULT_REGION.: ${AWS_DEFAULT_REGION}
AWS_ACCESS_KEY.....: ${AWS_ACCESS_KEY}
AWS_SECRET_KEY.....: ${AWS_SECRET_KEY:0:3}
AWS_BUCKET_NAME....: ${AWS_BUCKET_NAME}
AWS_BUCKET_REGION..: ${AWS_BUCKET_REGION}
EOF
