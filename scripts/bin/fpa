#!/bin/bash
FILE=$(
  fdfind \
    --type f \
    --hidden \
    --follow \
    --exclude .git \
| fzf \
    --multi \
    --preview 'batcat --style=numbers --color=always {} | head -50'
)

echo "${FILE}"
