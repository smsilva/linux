---
name: note-local
description: Appends a note directly to the project's local CLAUDE.local.md file (not committed to git)
---

Invoke the `note` skill with target `CLAUDE.local.md` and `<command-args>` as the note text.