#!/bin/bash

delete() {
  FILE_NAME=$1

  [ -e ${FILE_NAME?} ] && echo ${FILE_NAME?} && rm -rf ${FILE_NAME?}
}

delete .terraform
delete .terraform.*
delete terraform.tfstate
delete terraform.tfstate.backup
