#!/bin/bash
set -e

tfclear > /dev/null

ORIGIN_DIRECTORY=${PWD}

for FILE in $(find -name terragrunt.hcl | sort -u); do
  DIRECTORY=$(dirname ${FILE})

  cd ${DIRECTORY}
  pwd
  cd ${ORIGIN_DIRECTORY?}
done
